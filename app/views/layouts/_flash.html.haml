.container-fluid
  .row
    .col-lg-12
      - flash.each do |name, msg|
        %div{class: "alert alert-#{name.to_s == 'success' ? 'success' : 'danger'}"}
          %span.close{"data-dismiss" => "alert"} ×
          = msg
